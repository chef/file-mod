before:
  hooks:
    - go get -v -d ./...
builds:
  - id: "file-mod"
    binary: file-mod
    main: cmd/file-mod/main.go
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64